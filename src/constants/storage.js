/**
 * Manage all storage keys
 * KEY: such as UPLTV_USERNAME, 需要使用前缀区分不同项目的 Storage 存储
 * VALUE: UPLTV-USERNAME => storage's key
 */

export default {
  UPLTV_GLOBAL_TOKEN: 'UPLTV-GLOBAL-TOKEN', // login token
}
