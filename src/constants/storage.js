/**
 * Manage all storage keys
 * KEY: such as BRAND_USERNAME, 需要使用前缀区分不同项目的 Storage 存储
 * VALUE: BRAND-USERNAME => storage's key
 */

export default {
  GLOBAL_TOKEN: "GLOBAL-TOKEN" // login token
};
